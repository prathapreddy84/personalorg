global class memberApproval {
    webservice static void callApproval(id parid,id dupid) { 
              // Candidate__c newContact = new Candidate__c();
            //newContact.First_Name__c= firstName;
             //insert newContact;
                         
    }
    
}